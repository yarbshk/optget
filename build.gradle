allprojects {
    group 'com.github.yarbshk.optget'
    version '1.0-SNAPSHOT'

    tasks.withType(JavaCompile) {
        sourceCompatibility = 1.8
    }

    repositories {
        mavenCentral()
        maven {
            url 'http://maven.nuiton.org/release/' // com.sun.tools
        }
    }
}
